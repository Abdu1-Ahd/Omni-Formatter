{
  "name": "omnifmt-registry",
  "version": "0.1.0",
  "description": "OmniFormatter module registry server — Cloudflare Workers + Hono.js",
  "private": true,
  "scripts": {
    "build": "npx esbuild src/index.ts --bundle --outfile=dist/worker.js --platform=browser --format=esm --target=es2020 --external:__STATIC_CONTENT_MANIFEST",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "jest --testPathPattern='src/__tests__'",
    "lint": "eslint src/ --ext .ts"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.0.0"
  }
}
